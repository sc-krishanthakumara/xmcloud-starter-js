/**
 * Optional dependency referenced from generated `.sitecore/import-map.client.ts` when CDP/events are used.
 * Stub so `tsc` passes when `@sitecore-cloudsdk/events` is not installed.
 */
declare module '@sitecore-cloudsdk/events/browser' {
  export const pageView: unknown;
}
